---
description: Show jkit workflow help and route to the right command.
---

Use the `jkit` skill workflow from `skills/jkit/SKILL.md`.

Show concise jkit help:

- What jkit is.
- The recommended command flow.
- Which command to use next.
- How to check install state if commands or skills are missing.

Do not write specs, create plans, edit files, implement code, or run
verification unless the user explicitly asks for that action.
